# 200 requests per second per Host header. Excess → HTTP 429.
SecRuleEngine On
SecRule ARGS:id "@eq 1" "id:1, phase:1, pass, \
    ratelimit:zone[]=%{REQUEST_HEADERS.host}&events=200&window=1&action=deny&status=429, \
    status:200"
